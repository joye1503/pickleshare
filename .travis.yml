language: python
install: pip install -e .
script: py.test

python:
    - "3.7"

matrix:
  include:
    - python: "3.7"
      dist: xenial
      sudo: true
    - python: "3.7"
      arch: arm64
      dist: xenial
      sudo: true
    - python: "3.7"
      os: linux
      dist: xenial
      group: edge
      arch: arm64-graviton2
      virt: lxd
      sudo: true
